{
  "rules": {
    ".read": false,
    ".write": false,

    "profiles": {
      "$uid": {
        ".read": "auth !== null",
        ".write": "auth !== null && auth.uid === $uid"
      }
    },

    "rooms": {
      "$roomId": {
        ".read": "auth !== null",
        ".write": "auth !== null && ((!data.exists() && newData.child('players/white/uid').val() === auth.uid) || data.child('players/white/uid').val() === auth.uid || data.child('players/black/uid').val() === auth.uid || (!data.child('players/black').exists() && newData.child('players/black/uid').val() === auth.uid))"
      }
    }
  }
}
